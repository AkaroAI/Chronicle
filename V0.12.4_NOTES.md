# Chronicle v0.12.4

- Uses 3,500-character import segments with 300 characters of continuity overlap.
- Explicitly prevents the analyzer from rewriting or creatively expanding campaign prose.
- Requires compact, bounded field values while preserving each distinct durable fact.
- Produces more frequent checkpoints so interrupted local imports lose less work.
