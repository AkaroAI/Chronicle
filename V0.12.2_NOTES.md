# Chronicle v0.12.2

- Rejects conversational or incomplete AI replies during segmented campaign imports.
- Retries a malformed segment response up to three times with strict JSON instructions.
- Ignores invalid saved segment checkpoints so a bad response cannot poison later resumes.
- Accepts a valid JSON object even when a model accidentally wraps it in brief prose.
