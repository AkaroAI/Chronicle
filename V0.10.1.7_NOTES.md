# Chronicle v0.10.1.7

## Proposal scan route repair

- Routes background Review Inbox scans through the Chronicle Engine's OpenAI-compatible `/v1/chat/completions` endpoint.
- Preserves structured narration through `/api/v1/generate`.
- Adds regression tests for base URLs with and without a trailing `/v1`.

This fixes successful story replies being followed by silent `404 Not Found` failures during character, location, lore, and continuity proposal extraction.
